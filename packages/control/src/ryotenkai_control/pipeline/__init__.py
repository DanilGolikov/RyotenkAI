"""
Pipeline orchestration for RyotenkAI automated training system.
"""
# No top-level re-exports — importing sub-modules (state, stages, restart_points)
# must not trigger the heavy orchestrator/training stack as a side effect.
