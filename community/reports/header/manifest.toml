schema_version = 4

[plugin]
id = "header"
kind = "reports"
name = "Header"
version = "1.0.0"
category = "structural"
stability = "stable"
description = "Top-of-report identity block — run id, experiment name, status, health, duration."

[plugin.entry_point]
module = "plugin"
class = "HeaderBlockPlugin"
