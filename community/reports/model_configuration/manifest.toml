schema_version = 4

[plugin]
id = "model_configuration"
kind = "reports"
name = "Model Configuration"
version = "1.0.0"
category = "configuration"
stability = "stable"
description = "Base model name, dtype, device map, and adapter (LoRA / QLoRA) parameters."

[plugin.entry_point]
module = "plugin"
class = "ModelConfigurationBlockPlugin"
