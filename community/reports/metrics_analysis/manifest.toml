schema_version = 4

[plugin]
id = "metrics_analysis"
kind = "reports"
name = "Metrics Analysis"
version = "1.0.0"
category = "metrics"
stability = "stable"
description = "Loss curves, learning rate schedule, gradient norms — the headline training-progress charts and trends."

[plugin.entry_point]
module = "plugin"
class = "MetricsAnalysisBlockPlugin"
