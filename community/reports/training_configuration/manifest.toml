schema_version = 4

[plugin]
id = "training_configuration"
kind = "reports"
name = "Training Configuration"
version = "1.0.0"
category = "configuration"
stability = "stable"
description = "Per-strategy hyperparameters — epochs, learning rate, batch size, scheduler, mixed precision."

[plugin.entry_point]
module = "plugin"
class = "TrainingConfigurationBlockPlugin"
