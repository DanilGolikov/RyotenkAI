schema_version = 4

[plugin]
id = "dataset_validation"
kind = "reports"
name = "Dataset Validation"
version = "1.0.0"
category = "validation"
stability = "stable"
description = "Per-plugin validation results from the DatasetValidator stage — metrics, warnings, errors, recommendations."

[plugin.entry_point]
module = "plugin"
class = "DatasetValidationBlockPlugin"
