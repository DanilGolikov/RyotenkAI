schema_version = 4

[plugin]
id = "config_dump"
kind = "reports"
name = "Configuration"
version = "1.0.0"
category = "configuration"
stability = "stable"
description = "Verbatim YAML dump of the resolved pipeline config — a permanent record of what was actually trained against."

[plugin.entry_point]
module = "plugin"
class = "ConfigDumpBlockPlugin"
