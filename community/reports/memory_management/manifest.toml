schema_version = 4

[plugin]
id = "memory_management"
kind = "reports"
name = "Memory Management"
version = "1.0.0"
category = "diagnostics"
stability = "stable"
description = "GPU VRAM peaks, allocator events, and OOM near-misses captured during training."

[plugin.entry_point]
module = "plugin"
class = "MemoryManagementBlockPlugin"
