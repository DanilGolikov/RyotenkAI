"""
Domain-specific utility libraries.

Each sub-package contains parsing, extraction, and validation helpers
for a specific external domain. This code is NOT framework code —
it knows about domain concepts (HelixQL syntax, schema blocks, etc.)
and exists purely to support domain plugins.
"""
